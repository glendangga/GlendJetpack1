<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bangkit.glendjetpack1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GlendJetpack1">
        <activity android:name=".ui.reminder.ReminderActivity"></activity>
        <activity android:name=".ui.favorite.FavoriteActivity" />
        <activity
            android:name=".ui.detail.DetailShowActivity"
            android:label="@string/title_activity_detail_show"
            android:theme="@style/Theme.GlendJetpack1.NoActionBar" />
        <activity
            android:name=".ui.detail.DetailFilmActivity"
            android:label="@string/title_activity_detail_film"
            android:theme="@style/Theme.GlendJetpack1.NoActionBar" />
        <activity android:name=".ui.home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".ui.reminder.AlarmReceiver"
            android:enabled="true"
            android:permission="TODO"
            />

    </application>

</manifest>